# data 目录

这个目录用于存放本地生成的抓取、字幕、ASR、回填和导入中间资产。

开源仓库不提交原始 data/ 资产，因为这些文件可能包含：

- 字幕全文、ASR 转写全文和第三方接口响应；
- 浏览器、yt-dlp、Bilibili 等抓取过程中的错误日志；
- 本机路径、cookie 文件路径、调试输出和其他环境痕迹。

仓库只保留 data/samples/ 下的脱敏样例，方便理解数据格式。实际数据请在本地重新生成，并保持在 .gitignore 覆盖范围内。
